<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>문서 목록</title>
</head>
<body>
    <ul>
        <% for(let i = 0; i < files.length; i++) { 
            const file = files[i];
            %>
        <li>
            <a href="fileView?name=<%= file%>"><%= file%></a>
        </li>
        <% } %>
    </ul>
</body>
</html>